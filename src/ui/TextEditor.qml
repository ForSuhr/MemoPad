import QtQuick
import QtQuick.Window
import QtQuick.Controls
import QtQuick.Dialogs

Item {
    id: root
    width: 640
    height: 480
    visible: true

    //        FileDialog {
    //            id: openFileDialog
    //            title: qsTr("Select File")
    //            visible: true
    //            fileMode: FileDialog.OpenFile
    //            onAccepted: {
    //                textEditor.path = openFileDialog.file
    //            }
    //        }

    //        FileDialog {
    //            id: saveFileDialog
    //            title: qsTr("Save File")
    //            fileMode: FileDialog.SaveFile
    //            onAccepted: {
    //                textEditor.path = saveFileDialog.file
    //            }
    //        }
}
